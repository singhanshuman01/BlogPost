This is a simple Blog Posting website, developed using node.js and express. It doesn't use any database, I was just simply practicing to improve my backend and API skills.
Clone the repo using git clone command, and run npm i to install node dependencies. You should have node.js and npm installed on your local machine beforehand.
This project uses ESM(not CJS) so you can have trouble to run on <ESM6 versions.
